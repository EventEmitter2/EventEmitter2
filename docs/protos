//TODO  doc this
  //Constructor
  EventEmitter2(conf)
  //functioned used by the constructor
    initializeEvents = function(conf)
    setEventDelimiter = function(delimiter)
    setEventCaseSensitive = function(caseSensative)
    setMaxListeners = function(n)

  //error helper function
    eventNameError = function(fn)

//Listener related
  //Add Listener stub
  on = addListener;
    addListener = function(event, fn)
  //remove Listener stub
  un = removeListener;
    removeListener = function(event, fn)
  removeAllListeners = function()

  //Listen to ANY event
  onAny = function(fn)
  //remove Listener for onAny
  unAny = function(fn)

  //limited use listeners
  once = function(event, fn)
  many = function(event, ttl, fn)

  //get all listeners with this 'event'
  listeners = function(event)

//Emit related
  emit = function(event)
  //emit on All listeners
  emitAll = function()
